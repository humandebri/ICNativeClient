type CanisterInfoArgs = record {
  canister_id : principal;
  num_requested_changes : opt nat64;
};

type CanisterInfo = record {
  total_num_changes : nat64;
};

service : {
  canister_info : (CanisterInfoArgs) -> (CanisterInfo) query;
};
